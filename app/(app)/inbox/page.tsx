export const runtime = 'nodejs'

export default function InboxPage() {
  return (
    <div className="space-y-6">
      <h1>Inbox</h1>
      <p className="text-sm text-[hsl(var(--muted-foreground))]">
        Scaffold placeholder. Paste box + classify pipeline land in Step 5.
      </p>
    </div>
  )
}
